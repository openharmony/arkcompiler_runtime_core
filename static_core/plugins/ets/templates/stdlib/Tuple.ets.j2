/**
 * Copyright (c) 2025-2026 Huawei Device Co., Ltd.
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

// Autogenerated file. DO NOT EDIT

/**
 * @file Defines the Tuple for ArkTS
 * @kit ArkTS
 */
package std.core;

{% set maxarity=16 %}

{%- macro genTupleTypeParamsList(narg) -%}
    {%- if narg>0 %}<{% endif %}{% for i in range(narg-1) -%}
        T{{i}}, {{""}}
    {%- endfor -%}
    {%- if narg>0 %}T{{narg-1}}>{% endif %}
{%- endmacro %}

{%- macro genTupleConstructorParams(narg) -%}
    {% for i in range(narg-1) -%}
        p{{i}}: T{{i}}, {{""}}
    {%- endfor -%}
    {%- if narg>0 %}p{{narg-1}}: T{{narg-1}}{% endif %}
{%- endmacro %}

{%- macro genTupleConstructorArgs(narg) -%}
    {% for i in range(narg-1) -%}
        p{{i}}, {{""}}
    {%- endfor -%}
    {%- if narg>0 %}p{{narg-1}}{% endif %}
{%- endmacro %}

{%- macro genTupleNConstructorArgs() -%}
    {% for i in range(maxarity-1) -%}
         p[{{i}}] as T{{i}}, {{""}}
    {%- endfor -%}
    p[{{maxarity-1}}] as T{{maxarity-1}}
{%- endmacro %}

{%- macro genTupleClassComment(narg) -%}
/**
{%- if narg == 0 %}
 * Represents an empty tuple.
 * @implements Tuple
 {%- else %}
 * Represents a tuple containing {{ narg }} element{{ 's' if narg > 1 else '' }}.
 * @extends Tuple{{ narg-1 }}{{ genTupleTypeParamsList(narg-1) }}
{%- endif %}
 * @syscap SystemCapability.Utils.Lang
 * @FaAndStageModel
 */
{%- endmacro -%}

/**
 * Marker interface for tuple types.
 * @syscap SystemCapability.Utils.Lang
 * @FaAndStageModel
 */
export interface Tuple extends Iterable<Any>
{
    readonly length: int;

    /**
     * Get the element at the specified index.
     *
     * @param { int } index The index of the element to get.
     * @returns { Any } The element at the specified index.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    unsafeGet(index: int): Any;
}

class TupleIterator implements IterableIterator<Any> {
    private parent: Tuple
    private idx: int = 0

    constructor(parent: Tuple) {
        this.parent = parent
    }

    public override $_iterator(): IterableIterator<Any> {
        return this
    }

    override next(): IteratorResult<Any> {
        if (this.idx < 0 || this.idx >= this.parent.length) {
            return new IteratorResult<Any>()
        }
        return new IteratorResult<Any>(false, this.parent.unsafeGet(this.idx++));
    }
}

{{ genTupleClassComment(0) }}
export class Tuple0 implements Tuple
{
    constructor() {
    }

    public override toString(): string {
        return ""
    }

    /**
     * Get the length of the tuple.
     *
     * @returns { int } The length of the tuple.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public get length(): int {
        return 0;
    }

    /**
     * Get the element at the specified index.
     *
     * @param { int } index The index of the element to get.
     * @returns { Any } The element at the specified index.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public unsafeGet(index: int): Any {
        throw new IndexOutOfBoundsError();
    }

    /**
     * Iterable interface implementation
     *
     * @returns { IterableIterator<Any> } - iterator over all elements
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public override $_iterator(): IterableIterator<Any> {
       return new TupleIterator(this);
    }
}
{%- for narg in range(1, maxarity+1) %}

{{ genTupleClassComment(narg) }}
export class Tuple{{ narg }}{{ genTupleTypeParamsList(narg) }} extends Tuple{{narg-1}}{{ genTupleTypeParamsList(narg-1) }} {{ newline }}
{
    public ${{narg-1}}: T{{narg-1}}

    constructor({{genTupleConstructorParams(narg)}}) {
        super({{genTupleConstructorArgs(narg-1)}})
        this.${{narg-1}} = p{{narg-1}}
    }

    public override toString(): string {
        return super.toString() + {%- if narg > 1 %} "," + {%- endif %} this.${{narg-1}}
    }

    /**
     * Get the length of the tuple.
     *
     * @returns { int } The length of the tuple.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public override get length(): int {
        return {{narg}};
    }

    /**
     * Get the element at the specified index.
     *
     * @param { int } index The index of the element to get.
     * @returns { Any } The element at the specified index.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public override unsafeGet(index: int): Any {
      {%- if narg == 1 %}
        if (index == 0) {
            return this.$0;
        }
        throw new IndexOutOfBoundsError();
      {%- else %}
        switch (index) {
        {%- for ind in range(narg) %}
            case {{ind}}:
                return this.${{ind}}
        {%- endfor %}
            default:
                throw new IndexOutOfBoundsError();
        }
      {%- endif %}
    }
}
{%- endfor %}

/**
 * Represents a tuple containing an arbitrary number of elements greater than 16.
 * @extends Tuple{{maxarity}}{{ genTupleTypeParamsList(maxarity) }}
 * @syscap SystemCapability.Utils.Lang
 * @FaAndStageModel
 */
export class TupleN{{ genTupleTypeParamsList(maxarity) }} extends Tuple{{maxarity}}{{ genTupleTypeParamsList(maxarity) }}
{
    $tupleValues: Any[]

    constructor({{genTupleConstructorParams(maxarity)}}, p: Any[]) {
        super( {{genTupleConstructorArgs(maxarity) }})
        if (p.length == 0) {
           throw new AssertionError("Invalid number of tuple members. Use class 'Tuple{{maxarity}}' for this case.");
        }
        this.$tupleValues = p;
    }

    public override toString(): string {
        return super.toString() + "," + this.$tupleValues.toString();
    }

    /**
     * Get the length of the tuple.
     *
     * @returns { int } The length of the tuple.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public override get length(): int {
        return {{maxarity}} + this.$tupleValues.length;
    }

    /**
     * Get the element at the specified index.
     *
     * @param { int } index The index of the element to get.
     * @returns { Any } The element at the specified index.
     * @syscap SystemCapability.Utils.Lang
     * @FaAndStageModel
     */
    public override unsafeGet(index: int): Any {
        if (index >= {{maxarity}} && index < this.length) {
            return this.$tupleValues[index-{{maxarity}}];
        }
        return super.unsafeGet(index);
    }
}
