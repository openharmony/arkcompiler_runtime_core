# medium fixtures

Drop multi-file workloads here (a few hundred LOC). Files matching
`*.ts` (excluding `*.d.ts`) are auto-discovered by
[scenarios/declgen.bench.ts](../../scenarios/declgen.bench.ts).
