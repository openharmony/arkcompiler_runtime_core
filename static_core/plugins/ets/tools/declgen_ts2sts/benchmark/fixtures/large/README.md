# large fixtures

Drop heavy workloads here (thousands of LOC, or a real package).
Large fixtures should generally not be committed — prefer a generator
script or a symlink into `test/cookbook_tests/`.
