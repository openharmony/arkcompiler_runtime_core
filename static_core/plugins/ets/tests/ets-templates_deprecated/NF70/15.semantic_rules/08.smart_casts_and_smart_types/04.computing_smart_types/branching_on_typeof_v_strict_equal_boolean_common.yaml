# Copyright (c) 2026 Huawei Device Co., Ltd.
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

cases:
  # Note: each 'check_variant_decl_data'
  # define different types A and related to them check values for check
  # branching on "typeof v === "boolean"" where A is type of v variable
  - check_variant_decl_data: |-
      // type A is type of v variable used for check
      // branching on "typeof v === "boolean""
      type A = boolean | string

      // type A - boolean is computed smart type on the negative branch
      // after branching on "typeof v === "boolean""
      type A_diff_BooleanType = string

      // VarType is type of v variable after branching on
      // "typeof v === "boolean"": on positive branch, on negative branch
      enum VarType {BooleanType, A_diff_BooleanType}
      type CheckValueData = [
        A, // value
        VarType // value_type
      ]

      let check_values: ReadonlyArray<CheckValueData> = [
        [true, VarType.BooleanType],
        ["abc", VarType.A_diff_BooleanType] ]
      const check_values_number: int = 2
    check_value_by_expression_on_the_positive_branch:
      "arktest.assertEQ(v.valueOf(), true)"
    check_value_by_expression_on_the_negative_branch:
      "arktest.assertEQ(v.length, 3)"

  - check_variant_decl_data: |-
      // type A is type of v variable used for check
      // branching on "typeof v === "boolean""
      type A = long | int | byte | short | double | float |
        boolean | bigint | char | string | null | undefined

      // type A - boolean is computed smart type on the negative branch
      // after branching on "typeof v === "boolean""
      type A_diff_BooleanType = long | int | byte | short | double | float |
        bigint | char | string | null | undefined

      // VarType is type of v variable after branching on
      // "typeof v === "boolean"": on positive branch, on negative branch
      enum VarType {BooleanType, A_diff_BooleanType}
      type CheckValueData = [
        A, // value
        VarType // value_type
      ]

      let check_values: ReadonlyArray<CheckValueData> = [
        [false, VarType.BooleanType],
        [0x7FFF_FFFF_1, VarType.A_diff_BooleanType],
        [123, VarType.A_diff_BooleanType],
        [15 as byte, VarType.A_diff_BooleanType],
        [517 as short, VarType.A_diff_BooleanType],
        [4.59, VarType.A_diff_BooleanType],
        [17.8f, VarType.A_diff_BooleanType],
        [567n, VarType.A_diff_BooleanType],
        [c'a', VarType.A_diff_BooleanType],
        ["abc", VarType.A_diff_BooleanType],
        [null, VarType.A_diff_BooleanType],
        [undefined, VarType.A_diff_BooleanType] ]
      const check_values_number: int = 12
    check_value_by_expression_on_the_positive_branch:
      "arktest.assertEQ(v.valueOf(), false)"
    check_value_by_expression_on_the_negative_branch:
      "arktest.assertEQ(v, check_values[v_id][0])"

  - check_variant_decl_data: |-
      // type A is type of v variable used for check
      // branching on "typeof v === "boolean""
      type A = Object

      // type A - boolean is computed smart type on the negative branch
      // after branching on "typeof v === "boolean""
      type A_diff_BooleanType = Object // nearest supertype for A - boolean

      // VarType is type of v variable after branching on
      // "typeof v === "boolean"": on positive branch, on negative branch
      enum VarType {BooleanType, A_diff_BooleanType}
      type CheckValueData = [
        A, // value
        VarType // value_type
      ]

      let check_values: ReadonlyArray<CheckValueData> = [
        [true, VarType.BooleanType],
        [0x7FFF_FFFF_1, VarType.A_diff_BooleanType],
        [123, VarType.A_diff_BooleanType],
        [15 as byte, VarType.A_diff_BooleanType],
        [517 as short, VarType.A_diff_BooleanType],
        [4.59, VarType.A_diff_BooleanType],
        [17.8f, VarType.A_diff_BooleanType],
        [567n, VarType.A_diff_BooleanType],
        [c'a', VarType.A_diff_BooleanType],
        ["abc", VarType.A_diff_BooleanType] ]
      const check_values_number: int = 10
    check_value_by_expression_on_the_positive_branch:
      "arktest.assertEQ(v.valueOf(), true)"
    check_value_by_expression_on_the_negative_branch:
      "arktest.assertEQ(v, check_values[v_id][0])"

  - check_variant_decl_data: |-
      // type A is type of v variable used for check
      // branching on "typeof v === "boolean""
      type A = Any

      // type A - boolean is computed smart type on the negative branch
      // after branching on "typeof v === "boolean""
      type A_diff_BooleanType = Any // nearest supertype for A - boolean

      // VarType is type of v variable after branching on
      // "typeof v === "boolean"": on positive branch, on negative branch
      enum VarType {BooleanType, A_diff_BooleanType}
      type CheckValueData = [
        A, // value
        VarType // value_type
      ]

      let check_values: ReadonlyArray<CheckValueData> = [
        [true, VarType.BooleanType],
        [0x7FFF_FFFF_1, VarType.A_diff_BooleanType],
        [123, VarType.A_diff_BooleanType],
        [15 as byte, VarType.A_diff_BooleanType],
        [517 as short, VarType.A_diff_BooleanType],
        [4.59, VarType.A_diff_BooleanType],
        [17.8f, VarType.A_diff_BooleanType],
        [567n, VarType.A_diff_BooleanType],
        [c'a', VarType.A_diff_BooleanType],
        ["abc", VarType.A_diff_BooleanType],
        [null, VarType.A_diff_BooleanType],
        [undefined, VarType.A_diff_BooleanType] ]
      const check_values_number: int = 12
    check_value_by_expression_on_the_positive_branch:
      "arktest.assertEQ(v.valueOf(), true)"
    check_value_by_expression_on_the_negative_branch:
      "arktest.assertEQ(v, check_values[v_id][0])"
