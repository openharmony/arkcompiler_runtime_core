{
    "compilerOptions": {
        "package": "@panda/entry",
        "baseUrl": "${CURRENT_SOURCE_DIR}",
        "rootDir": "${CURRENT_SOURCE_DIR}",
        "cacheDir": "${DEFAULT_CACHE_DIR}",
        "paths": {
            "std": ["${PANDA_ROOT}/plugins/ets/stdlib/std"]
        },
        "dependencies": {
            "@panda/lib":  { "path": "${CURRENT_BINARY_DIR}/z.abc"  }
        }
    }
}
