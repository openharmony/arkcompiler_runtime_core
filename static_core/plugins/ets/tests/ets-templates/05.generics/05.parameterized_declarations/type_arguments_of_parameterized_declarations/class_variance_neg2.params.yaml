---
cases:
  - use: |-
        let a: A<in X> = new A<X>(new X())
        let z: X = a.methOut()

  - use: |-
        let a: A<in X> = new A<X>(new X())
        let z: X = a.methInOut(new X())

  - use: |-
        let a: A<in X> = new A<X>(new Y())
        let z: Z = a.methOut()

  - use: |-
        let a: A<out X> = new A<X>(new X())
        a.methIn(new X())

  - use: |-
        let a: A<out X> = new A<X>(new X())
        let z: X = a.methInOut(new X())

  - use: |-
        let a: A<out> = new A<Object|null>(new X())
        a.methIn(new X())

  - use: |-
        let a: A<out> = new A<Object|null>(new X())
        let z: Object|null = a.methInOut(new X())
