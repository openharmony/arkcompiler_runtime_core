/*---
desc: Multi-line string literals that can include embedded expressions are called Template literals
tags: [compile-only]
---*/

let sentence = `This is an example of multi-line string,
 which should be enclosed in backticks (\`)`
