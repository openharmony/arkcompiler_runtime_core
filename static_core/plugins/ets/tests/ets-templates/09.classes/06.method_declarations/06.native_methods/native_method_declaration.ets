/*---
desc: Native method declaraion.
assert: A method that is native is implemented in platform-dependent code, typically written in another programming language such as C.
tags: [compile-only]
---*/

abstract class A {
  native run(): void;
}