/*---
desc: Abstract method declaraion.
assert: An abstrac method declaration introduces the method as a member, providing its signature, but does not provide an implementation.
tags: [compile-only]
---*/

abstract class A {
  abstract run(): void;
}
