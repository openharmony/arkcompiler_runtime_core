/*---
desc: Declaring iabstract method in interface with semicolon.
assert: An abstract method of an interface does not have a body represented by a block of code.
tags: [compile-only]
---*/

interface A {
  m(): int;
}